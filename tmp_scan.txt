26: * 选择 `dist/pose_capture-1.3.1-legacy.zip`
31: 把 `dist/pose_capture-1.3.1-extension.zip` 解压到（目录名必须是 `pose_capture`）：
68: > **1.3.0**：`IK 控制器` 模式改成**直接写进 IK 控制器**（不切 FK、不调 Rigify 的快照操作符）：
81: > **1.3.1**：修“**深度图里手在前面，应用后手却跑到身体后面**”（肩 / 手最明显）。四个成因一起修：
216: * **整张图前后反了**：正常情况下方向由拟合自动决定，拟合分不出前后时由**物理判据**（人比背景近）兜底定符号；确实反了才勾 `反转深度方向`（1.3.1 起这个开关真的会把整套姿态前后翻过来；勾了以后就不用物理判据了，物理判据不适用时用它），勾错会自动退回骨长解算
